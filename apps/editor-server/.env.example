# Server Configuration
PORT=3001

# Workspace Configuration
# Path to the root directory for file operations
# Default: current working directory
WORKSPACE_ROOT=.

# Chat Agent API Keys
ANTHROPIC_API_KEY=

# CORS Configuration
# Allowed origins (use '*' for all, or specify comma-separated origins)
# Example: http://localhost:5173,http://localhost:3000
CORS_ORIGIN=*

# CORS Credentials
# Set to 'true' to allow credentials (cookies, authorization headers)
CORS_CREDENTIALS=true
